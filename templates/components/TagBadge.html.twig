<a href="{{ path('app_tag_show', {'slug': tag.slug}) }}" class="{{ class|default('badge bg-primary-subtle text-primary border border-primary-subtle px-2 py-1 text-decoration-none') }}">
    <i class="bi bi-tag me-1"></i> {{ tag.name }}
</a>
