<nav class="navbar navbar-expand-lg container">
    <a class="navbar-brand fw-bold text-primary" href="{{ path('app_home') }}">
        <i class="bi bi-code-slash me-2"></i>Les Chroniques du Code
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <form action="{{ app.request.attributes.get('_route') == 'app_tag_show' ? path('app_tag_show', {slug: app.request.attributes.get('_route_params')['slug']}) : path('app_post_index') }}" method="get" class="d-flex mx-auto mt-2 mt-lg-0" style="max-width: 400px;">
            <div class="input-group">
                <input type="text" name="q" class="form-control" placeholder="Rechercher..." value="{{ app.request.query.get('q') }}">
                <button class="btn btn-outline-primary" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </div>
        </form>
        <ul class="navbar-nav ms-auto align-items-lg-center">
            <li class="nav-item">
                <a class="nav-link transition-base {{ app.request.attributes.get('_route') == 'app_home' ? 'active fw-bold' : '' }}" href="{{ path('app_home') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link transition-base {{ app.request.attributes.get('_route') starts with 'app_post' ? 'active fw-bold' : '' }}" href="{{ path('app_post_index') }}">Articles</a>
            </li>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link transition-base {{ app.request.attributes.get('_route') == 'app_post_admin_index' ? 'active fw-bold' : '' }}" href="{{ path('app_post_admin_index') }}">
                            <i class="bi bi-speedometer2 me-1"></i>Admin
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle transition-base {{ app.request.attributes.get('_route') starts with 'app_tag' ? 'active fw-bold' : '' }}" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-tags me-1"></i>Tags
                        </a>
                        <ul class="dropdown-menu shadow-sm border-0">
                            <li><a class="dropdown-item" href="{{ path('app_tag_index') }}"><i class="bi bi-list-ul me-2"></i>Gérer les tags</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_tag_new') }}"><i class="bi bi-plus-circle me-2"></i>Nouveau Tag</a></li>
                        </ul>
                    </li>
                {% endif %}
                <li class="nav-item ms-lg-3">
                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle transition-base" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-1"></i>{{ app.user.userIdentifier }}
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Déconnexion</a></li>
                        </ul>
                    </div>
                </li>
            {% else %}
                <li class="nav-item ms-lg-3">
                    <a class="nav-link transition-base {{ app.request.attributes.get('_route') == 'app_login' ? 'active fw-bold' : '' }}" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item ms-lg-2">
                    <a class="btn btn-primary transition-base" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
