<!DOCTYPE html>
<html lang="fr-FR" data-bs-theme="auto">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="Les Chroniques du Code - Un blog dédié au développement web, à la passion du code et au partage de connaissances.">
        <title>{% block title %}Les Chroniques du Code{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header class="bg-body-tertiary shadow-sm mb-4">
            <nav class="navbar navbar-expand-lg container">
                <a class="navbar-brand fw-bold text-primary" href="{{ path('app_post_index') }}">
                    <i class="bi bi-code-slash me-2"></i>Les Chroniques du Code
                </a>
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link transition-base" href="{{ path('app_post_index') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-primary ms-lg-3 transition-base" href="{{ path('app_post_new') }}">
                                <i class="bi bi-plus-circle me-1"></i>Nouveau Post
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        </header>

        <main class="container py-4">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show animate-fade-in" role="alert">
                        <i class="bi {{ label == 'success' ? 'bi-check-circle' : 'bi-exclamation-triangle' }} me-2"></i>
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer class="bg-dark text-white py-5 mt-5">
            <div class="container text-center">
                <p class="mb-2 fw-bold">Les Chroniques du Code &copy; {{ "now"|date("Y") }}</p>
                <p class="text-muted small mb-0">Développé avec passion pour le web inclusif et responsive.</p>
                <div class="mt-3">
                    <a href="#" class="text-white-50 me-3 transition-base" aria-label="Lien vers Twitter"><i class="bi bi-twitter"></i></a>
                    <a href="#" class="text-white-50 transition-base" aria-label="Lien vers GitHub"><i class="bi bi-github"></i></a>
                </div>
            </div>
        </footer>
    </body>
</html>
